version: "3.7"

services:
  db_todo-api:
    image: postgres
    volumes:
      - /postgres:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: todo_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
